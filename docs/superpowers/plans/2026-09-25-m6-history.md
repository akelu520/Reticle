# M6 截图历史 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 设计文档 4.8：最近 20 张截图，菜单栏重新复制或打开。

**Architecture:** `ScreenshotHistoryStore`（ReticleCore）管理目录、索引、缩略图、上限与恢复；App 层 `ScreenshotHistory` 把磁盘操作放到后台串行队列，主线程保存条目副本；`HistoryMenu` 在菜单打开时构建、关闭时释放。

---

- [x] Core：`ScreenshotHistory/ScreenshotHistoryStore.swift`；测试 7 个（顺序、缩略图尺寸、上限删文件、跨实例持久化、损坏索引与孤儿文件、图片缺失、删除/清空、非图片数据）
- [x] 修复单元测试发现的问题：索引编码/解码日期策略不一致 → 重启后历史读不出且图片被当孤儿删除；统一为默认编码（亚秒精度完整往返）
- [x] App：`App/ScreenshotHistory/{ScreenshotHistory,HistoryMenu}.swift`；记录点：CaptureSession 复制/下载/固定、LongImageWindow 复制/保存
- [x] 设置“保存截图历史”（默认开）；`Preferences.historyEnabled`
- [x] E2E“截图历史” 11 项；`scripts/e2e.sh` 使用临时历史目录

### 验收记录（2026-09-25）
- `swift test`：66 tests, 0 failures；`scripts/e2e.sh`：116/116 通过
- 历史相关操作后主进程 46 MB（Debug，单独进程）；Release 空闲 11 MB；`.app` 2.3 MB，DMG 1.3 MB
