# M3 贴图 + 提取文字 + 翻译 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 设计文档 4.4 / 4.5：固定到屏幕、提取文字（两种入口、可编辑、复制、链接、关闭重选）、macOS 15+ 翻译。

**Architecture:** OCR（Vision）、阅读顺序、链接识别、默认目标语言放在 ReticleCore 并单测；App 层新增 `PinWindow`、`TextRecognitionPanel`、`Translator`（SwiftUI `.translationTask` 桥接）和顶部 `ModeBar`。

**Tech Stack:** Vision、NaturalLanguage、Translation（macOS 15+）、AppKit。

---

### Task 1: Core ✅
- `TextRecognition/ReadingOrder.swift`：按行分组、行内从左到右、CJK 不加空格
- `TextRecognition/TextRecognizer.swift`：`.accurate` + 语言纠错，语言取系统支持的 zh-Hans/zh-Hant/en/ja/ko
- `TextRecognition/TextAnalysis.swift`：http/https 链接识别；中文→英文、其他→简体中文
- `CoordinateSpace.appKitGlobal(fromLocalFlipped:screenFrame:)`
- 测试：`TextRecognitionTests`（含真实 Vision 识别渲染文字）、`AppKitGlobalTests`

### Task 2: 固定到屏幕 ✅
`App/Pin/PinWindow.swift`：非激活浮动面板，所有桌面可见；拖动、双击/Esc 关闭、右键 复制/保存…/关闭；`PinManager` 持有窗口。

### Task 3: 提取文字与翻译 ✅
- `App/TextRecognition/TextRecognitionPanel.swift`：识别中/未识别到文字/失败状态；可编辑文本；链接可点击（打开后结束截图会话）；复制全文；关闭 = 清空选区重新框选
- `App/TextRecognition/Translator.swift`：`TranslationSession` 桥接；目标语言下拉；原文/译文切换
- 入口：顶部模式栏“提取文字”（框选完成自动识别，隐藏工具栏）、主工具栏“提取文字”、菜单栏“提取文字”

### 验收记录（2026-09-25）
- `swift test`：46 tests, 0 failures
- 离屏渲染：模式栏、带新按钮的工具栏、OCR 面板（演示图识别出 “Reticle demo”）
- Release `.app` 1.0 MB，DMG 748 KB，空闲 11 MB
- 未验证（需真机）：翻译（依赖系统语言包下载弹窗）、贴图窗口拖动和右键菜单、链接跳转
